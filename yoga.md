💬 2️⃣ /chat → Chat Page

Assigned: 💻 Yoga
Goal: Create the text chat UI between user ↔ Sarathi.

UI Style:

Minimal chat box in center.

Left = AI messages (blue bubble with Krishna emoji or chariot icon).

Right = User messages (gray or white bubble).

Input bar at bottom → send message.

Auto scroll + typing indicator dots.

Work for Yoga:

Build full chat layout with React state.

Call /api/chat → display AI reply.

Add loading animation (3 dots).

Optional: Use Framer Motion for smooth message entry.